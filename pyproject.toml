[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"[[roject]
name = "ligeia-mcp-server"
version = "1.0.0"
description = "Autonomous, edge-native micro-commerce computational engines monetized via x402 on Base Mainnet."
requires-python = ">=3.10"
